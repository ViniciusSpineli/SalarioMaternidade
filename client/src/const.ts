export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

// Login local: a autenticação é feita pela própria aplicação (email/senha),
// então a "URL de login" é a nossa tela interna.
export const getLoginUrl = () => "/login";
